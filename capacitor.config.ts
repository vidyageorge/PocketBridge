import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sixtycubits.pocketbridge',
  appName: 'PocketBridge',
  webDir: 'dist',
};

export default config;
